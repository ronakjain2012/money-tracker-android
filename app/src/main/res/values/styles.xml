<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="GateButtons" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textSize">20dp</item>
        <item name="android:textColor">#4348CF</item>
        <item name="android:background">@drawable/gate_buttons</item>
    </style>

    <style name="BasicButtons" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textSize">16dp</item>
        <item name="android:color">#4348CF</item>
        <item name="android:background">@drawable/basic_buttons_white</item>
        <item name="android:layout_margin">5dp</item>
    </style>

    <style name="QuickButtons" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textSize">16dp</item>
        <item name="android:color">#4348CF</item>
        <item name="android:background">@drawable/quick_buttons</item>
        <item name="android:layout_margin">5dp</item>
    </style>
</resources>
